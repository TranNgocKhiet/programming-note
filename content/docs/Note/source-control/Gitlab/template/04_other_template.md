---
title: "Other Template"
weight: 4
---

# Other Template

```yml
Jobs/Code-Quality.gitlab-ci.yml
```

- Quét code để tìm các vấn đề về "chất lượng" (độ phức tạp, code trùng lặp, lỗi cú pháp) thay vì lỗ hổng bảo mật

```yml
Jobs/Browser-Performance.gitlab-ci.yml
```

- Kiểm tra hiệu suất (tốc độ tải) của trang web sau khi deploy